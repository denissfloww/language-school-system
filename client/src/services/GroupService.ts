const getGroups = () => {
    const groups = [
        {
            id: 1,
            name: 'Англ',
            desc: 'Группа по англ языку',
        },
    ];

    return groups
};

const GroupService = {
    getGroups,
};
export default GroupService;
